#include <stdio.h>
#include <string.h>

int main()
{
	char searched_string[255] = "";
	char path_to_file[255] = "";
	int quantity = 0;
	printf("Insert path to your file (use \"\" to enter the path)");
	scanf("\s",&path_to_file);
	printf("Insert searched string (use \"\" to enter the path)");
	scanf("\s",&searched_string);
	printf("\s\n\s", path_to_file, searched_string);




}
